interface:
  display_name: "Freshdesk Ticket Assignment Helper"
  short_description: "Read-only triage with supervised Customer Service Group assignment"
  default_prompt: "Use $freshdesk-ticket-assignment-helper for read-only triage by default. After showing a successful dry-run for the current CS batch, accept the user's plain confirmation to run its guarded action while leaving Agent empty."
