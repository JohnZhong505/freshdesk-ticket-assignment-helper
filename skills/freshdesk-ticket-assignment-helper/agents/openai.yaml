interface:
  display_name: "Freshdesk Ticket Assignment Helper"
  short_description: "Dual-view triage, Cron cards, and supervised fixed-route assignment"
  default_prompt: "Use $freshdesk-ticket-assignment-helper for read-only Technical Service or Customer Service triage by default. Unattended Cron may classify and send fixed-recipient DingTalk cards but never write Freshdesk. After an interactive assignment dry-run for the latest displayed batch, accept the user's plain confirmation for that exact batch and fixed route while leaving Agent empty."
